'use strict';

// import 'socialfeed'

export default class FbFeed {
	constructor() {
		
		// var fbAccessToken = $('#facebook').data('fbtoken'),
		// 	fbTemplate = '<article class="feed {{? !it.moderation_passed}}hidden{{?}}"><span class="date">{{=it.date}}</span>{{=it.attachment}}<p><span class="text">{{=it.text}}</span><a class="btn btn--plain link" href="{{=it.link}}" target="_blank"><span><span class="btn__text">Read More</span><i class="icon icon-right-arrow"></i></span></a></p></article>';
		
		// $('#facebook').socialfeed({
		// 	facebook: {
		// 		accounts: ['@BeanstalkSingapore'],
		// 		// accounts: ['@sutdsingapore'],
		// 		limit: 3,
		// 		access_token: fbAccessToken,
		// 	},
		// 	template_html: fbTemplate,
		// 	length: 300,
		// 	show_media: true
		// });
	}
}
